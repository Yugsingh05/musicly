import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
 images : {
  domains : ['images.unsplash.com','cdn.siasat.com','th.bing.com','images.genius.com']
 }
};

export default nextConfig;
